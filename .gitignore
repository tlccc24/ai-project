.aider*

# Python
__pycache__/
*.pyc

# Local secrets / env
.env

# Databases (created at runtime; on Render they live on the persistent disk)
*.db
*.db.bak

# Uploaded reference photos (stored on the persistent disk at runtime)
uploads/
